import styled from "styled-components";

const Title = styled.blockquote`
  text-transform: uppercase;
  font-weight: bold;
  letter-spacing: 2px;
  font-size: 2.1rem;
  color: var(--color-grey-500);
  border-right: 4px solid var(--color-grey-300);
  padding: 0 1rem;
`;
export default Title;